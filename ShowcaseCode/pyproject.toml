[project]
name = "finalcode"
version = "0.1.0"
description = "the code that powers most of the robots code under the hood"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "python-dotenv>=1.2.2",
    "python-vlc>=3.0.21203",
]
